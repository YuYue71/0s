### 1. 角色設定與核心目標
> **Role:** 你是一位精通平面 UI 設計（Flat UI Design）與互動前端開發的專家。
> **Task:** 請根據我提供的「基礎 HTML/CSS 模板」以及「教學內容主題」，生成一個完整的展示教學網頁。

### 2. 視覺與設計規範（核心約束）
*   **圖標庫替代方案：** 絕對禁止使用任何 Emoji 圖標。請統一使用 `lucide-react` 圖標庫（如模板所示，使用 `<i data-lucide="icon-name"></i>`），並根據內容選擇最直觀的平面圖標。
*   **視覺風格還原：** 
    *   嚴格遵循深色系背景（`#0a0a0a`）與高對比強調色。
    *   精確還原「Screenshot_2026-05-05-14-08-39-61_f9ee0578fe1cc94de7482bd41accb329.jpg」中的佈局結構、元素間距與字體層次。
    *   交互元素（如卡片、按鈕）必須維持原有的顏色方案與視覺一致性。
*   **動態流動效果：** 所有的邏輯連接線必須使用帶有循環流動動畫（`@keyframes flow`）的偽虛線箭頭 SVG。確保箭頭在初始呈現後能保持動態效果。

### 3. 生成規則與結構細節
*   **動態頁數與長度：** 網頁的區塊數量（`.layer-box` 與節點）必須根據教學主題的複雜度進行動態調整。如果步驟較多，請自動增加對應的層級容器與連接箭頭。
*   **邏輯分層：** 教學內容應被邏輯性地拆分為「Input」、「Process/Problem Layer」與「Output/Solution Layer」，並賦予對應的 Accent Color（如紅色系代表問題，綠色系代表解決方案）。
*   **響應式要求：** 確保生成的代碼能適應不同尺寸的螢幕，在行動端應自動轉化為垂直流式佈局。

### 4. 交付物要求
*   **完整代碼：** 請直接輸出完整的 HTML 文件，包含內嵌的 CSS 與初始化 Script。
*   **視覺檢查點：** 請在生成後自我檢查，確保視覺效果與參考圖片的相似度達到 95% 以上。忽略原始截圖中右下角的浮水印或無關色彩塊。

---

## 注意事項與調優建議

在使用這份提示詞時，請留意以下幾點以獲得最佳產出：

*   **教學內容的結構化：** 餵給 AI 主題時，最好先將內容簡單條列化（例如：輸入是什麼、遇到的困難、解決的技術、最終結果）。這能幫助 AI 更準確地將內容填入對應的 UI 區塊。
*   **強調「忽略右下角」：** 雖然提示詞已提及，但若 AI 誤生成了多餘的區塊，請再次提醒它忽略截圖中的外部創作者標識，專注於 UI 框架。
*   **圖標名稱準確性：** 有時 AI 可能會發明不存在的圖標名稱。建議提醒它：「若不確定 Lucide 圖標名稱，請優先使用 `box`, `cpu`, `activity`, `database` 等通用技術圖標」。