## 一、請說明以下三點（用自己的話寫清楚即可）：
- 1.Python 的 `input()` 回傳值是什麼資料型態？
    - 為什麼這件事很重要？
    ##### 答案:
    ```bash
    使用者輸入,預設為字串型態(str)
    假如要將使用者輸入之資料進行計算等操作需要用 int(str) 將字串轉為整數型態,否則無法進行計算,會報錯
    ```

- 2.`int()` 與 `float()` 轉型各自適用什麼情境？
    - 請各舉 1 個例子說明。
    ##### 答案:
    ```bash
    int() 轉型適用於將其他型態轉為 整數 型態並進行 整數 運算
    float() 轉型適用於將其他型態轉為 單浮點數(小數點後7位) 型態並進行 浮點數 運算
    ```
    ##### 範例:
    ```python
    x = int(input("輸入價格:"))     # 將使用者輸入之價格轉為int整數型態,並存入 x 變數中
    print(f"價格為: {x:>4} 元")     # 印出 x 變數
    # 輸入: 66
    # 輸出: 價格為:   66 元
    ```
    ```python
    x = float(input("輸入價格:"))   # 將使用者輸入之價格轉為float浮點數型態,並存入 x 變數中
    print(f"價格為: {x:>4} 元")     # 印出 x 變數
    # 輸入: 77.6
    # 輸出: 價格為: 77.6 元
    # 如果再 float() 外在包一層 int() 可以做到輸入浮點數輸出整數的效果,直接用含小數點的字串轉整數會報錯
    ```

- 3.當使用者輸入的內容不符合轉型格式時，可能會出現什麼錯誤？
    - 錯誤代表什麼意思？
    ##### 答案:
    ```bash
    ValueError
    代表傳入的型態正確,但具體數值不正確
    ```



## 二、請比較並說明以下三種輸出方式差異，並說明「你會在什麼情況選用它」：
- 1.`print("總價:", total)`
- 2.`print("總價 = " + total)`
- 3.`print(f"總價 = {total}")`
    ##### 答案:
    ```bash
    多參數輸出
    輸出自動將逗號變為空格,且會將 total 變數自動轉為字串輸出
    快速除錯或單純想印出多個變數時使用
    ```
    ```bash
    字串拼接
    total必須先轉為字串才可拼接,否則會報TypeError
    難用,幾乎不會使用
    ```
    ```bash
    字串格式化
    效能最高,可以直接在 {"大括號"} 中寫入運算式等等
    超愛用,說實話我連除錯都用它,大多情況下都可以用它,很萬用
    ```



## 三、請用「自己的話」解釋：
- 1.`//`（整除）與 `%`（餘數）各自代表什麼意思？
- 2.你會如何用 `//` 與 `%` 解決「找零」或「時間換算」這種問題？
    - ###### （請以具體例子說明，例如 73 秒換算成 1 分 13 秒，或 289 元分成 100/10/1 的硬幣數量。）
    ##### 答案:
    ```bash
    // (整除) 將兩數相除後四捨五入取整數
    % (餘數) 取兩數相除後的餘數
    ```
    ```python
    x = int(input("輸入硬幣數:"))   # 設輸入289
    y = x // 100 * 100      # 計算百位數 289//100=2, 2*100=200
    z = x % 100 // 10 * 10  # 計算十位數 289%100//10=8, 8*10=80
    i = x % 100 % 10        # 計算個位數 289%100%10=9
    print(f"個: {i:>3} 元\n十: {z:>3} 元\n百: {y:>3} 元")
    # 輸出如下:
    # 輸入硬幣數:289
    # 個:   9 元
    # 十:  80 元
    # 百: 200 元
    ```



## 四、請撰寫「超商結帳小票」程式：
- 輸入商品名稱（字串）`item`
- 輸入單價（整數）`price`
- 輸入數量（整數）`qty`
##### 計算 `total = price * qty`，並輸出如下格式（對齊不要求，但資訊要完整）：
- ###### (商品：xxx 、單價：xxx 元 、數量：xxx 個 、總價：xxx 元)
    ##### 答案:
    ```python
    item = input("輸入商品名稱:")           # 獲得商品名稱輸入 (設輸入RAM)
    price = int(input("輸入商品單價:"))     # 獲得商品單價輸入 (設輸入99999)
    qty = int(input("輸入購買數量:"))       # 獲得購買數量輸入 (設輸入2)
    total = price * qty                    # 計算總價
    print(f"商品: {item}\n單價: {price:>6} 元\n數量: {qty:>6} 個\n總價: {total:>6} 元")
    # 輸出如下:
    # 商品: RAM
    # 單價:  99999 元
    # 數量:      2 個
    # 總價: 199998 元
    ```



## 五、請撰寫程式，輸入一個整數秒數 s，輸出：
- `m 分 r 秒`其中：
    - `m = s // 60`
    - `r = s % 60`
- ###### (輸出格式範例：輸入：73 → 輸出：1 分 13 秒)
    ##### 答案:
    ```python
    s = int(input("輸入秒數:"))  # 獲得秒數輸入 (設輸入73)
    m = s // 60     # 計算分鐘數 (73秒整除60秒等於1分鐘)
    r = s % 60      # 計算剩餘秒數 (73秒除60秒的餘數等於13秒)
    print(f"輸出: {m} 分 {r} 秒")
    # 輸出如下:
    # 輸出: 1 分 13 秒
    ```